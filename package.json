{
  "name": "open-gsio",
  "module": "index.ts",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run ./server.ts",
    "test": "bun --filter '*' vitest"
  },
  "workspaces": [
    "packages/*",
    "packages/cloudflare-workers/*"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "dotenv": "^16.5.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "bun-sqlite-key-value": "^1.13.1"
  }
}
